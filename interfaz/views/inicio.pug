extends layout3

block content
  h1= title
  table
    form(action="/logueo" method="GET")
        tr
            th
                input(type="text" name="user" placeholder="Ingrese el Usuario")
        tr
            th
                input(type="text" name="pass" placeholder="Ingrese la contraseña")
        tr
            th
                input(type="submit" value="Ingresar" class="glyphicon glyphicon-user btn-lg btn-success")